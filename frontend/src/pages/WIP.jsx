import React from 'react';
import Searchbar from '../components/Searchbar';

const WorkInProgress = () => {
    return (
        <div>
            <h1>WIP</h1>
            <p>Initial version of  frontend React JS  app has been developed.</p>
<p>Basic Blog app has been deployed with Google Cloud</p>
<p>Both Frontend and Backend docker images are running on GCP</p>
<p>Work in progres on CI/CD for multiple dev environments</p>
<p>Work in progress for Slackbot</p>
<p>Work in progress for Unit testing. 79% coverage for Model package.</p>

            <div>
                <Searchbar />
            </div>
        </div>
    );
};

export default WorkInProgress;