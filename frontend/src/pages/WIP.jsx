import React from 'react';

const WorkInProgress = () => {
    return (
        <div>
            <h1>WIP</h1>
            <p>Initial version of  frontend React JS  app has been developed.</p>
<p>Frontend app has been deployed with Google Cloud</p>
<p>Docker container has been setup and used for deployment</p>
<p>Nginx configurations for serving the app has been completed</p>
<p>Basic CI/CD pipeline for building and deploying the app for prod environment has been made using Google Cloud Build and Google Cloud Run. </p>
<p>Backend API work is in progress. 70% completed</p>
<p>Database setup completed.</p>

        </div>
    );
};

export default WorkInProgress;